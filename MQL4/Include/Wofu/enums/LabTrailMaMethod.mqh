enum ENUM_MA_METHOD
{
   E_MODE_SMA = MODE_SMA,  // Simple averaging
   E_MODE_EMA = MODE_EMA,  // Exponential averaging
   E_MODE_SMMA = MODE_SMMA,  // Smoothed averaging
   E_MODE_LWMA = MODE_LWMA  // Linear-weighted averaging
};